# CLAUDE.md

## プロジェクト概要

このファイルは、本プロジェクトで作業する AI アシスタント向けの開発ルールです。
プロジェクト固有の技術スタックや実行方法が追加された場合は、このファイルにも反映してください。

## 基本方針

- 変更前に関連するファイルと既存の実装を確認する。
- 依頼された範囲に変更を限定し、無関係なリファクタリングは行わない。
- 既存の命名、構成、コーディングスタイルを優先する。
- 変更後は、利用可能なテスト、ビルド、Lint、型チェックを実行する。
- 実行した検証と、未実施の検証があれば作業結果に明記する。
- 機密情報や認証情報をソースコード、ログ、コミットに含めない。

## Git 運用ルール

- コードを変更するたびに、変更内容を確認してコミットする。
- コード変更のコミット後は、必ず GitHub の対象リモートブランチへプッシュする。
- プッシュ前に、対象ブランチと `git diff`、コミット対象ファイルを確認する。
- 既存のユーザー変更を無断で破棄しない。
- 競合、認証エラー、リモート未設定などでプッシュできない場合は、原因を報告し、成功するまで完了扱いにしない。
- 強制プッシュ (`git push --force` など) は、明示的な依頼がない限り実行しない。

## 作業手順

1. 関連ファイル、Git の状態、現在のブランチを確認する。
2. 必要最小限の変更を実装する。
3. テストやビルドなどの検証を実行する。
4. 差分を確認し、変更をコミットする。
5. コミットを GitHub へプッシュする。
6. 変更内容、検証結果、コミット、プッシュ結果を報告する。
